<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shopping.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="com.shopping.entity.OrderInfo">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="receiver_name" property="receiverName"/>
        <result column="receiver_phone" property="receiverPhone"/>
        <result column="receiver_address" property="receiverAddress"/>
        <result column="total_price" property="totalPrice"/>
        <result column="status" property="status"/>
        <result column="payment_time" property="paymentTime"/>
        <result column="send_time" property="sendTime"/>
        <result column="end_time" property="endTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="OrderInfo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_info (order_no, user_id, receiver_name, receiver_phone, receiver_address, total_price, status)
        VALUES (#{orderNo}, #{userId}, #{receiverName}, #{receiverPhone}, #{receiverAddress}, #{totalPrice}, #{status})
    </insert>

    <update id="updateById" parameterType="OrderInfo">
        UPDATE order_info
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="paymentTime != null">payment_time = #{paymentTime},</if>
            <if test="sendTime != null">send_time = #{sendTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM order_info WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="long" resultMap="BaseResultMap">
        SELECT * FROM order_info WHERE id = #{id}
    </select>

    <select id="selectByOrderNo" parameterType="string" resultMap="BaseResultMap">
        SELECT * FROM order_info WHERE order_no = #{orderNo}
    </select>

    <select id="selectByUserId" parameterType="long" resultMap="BaseResultMap">
        SELECT * FROM order_info WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM order_info ORDER BY create_time DESC
    </select>

    <update id="updateStatus">
        UPDATE order_info SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
