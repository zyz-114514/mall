<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shopping.mapper.CartMapper">

    <resultMap id="BaseResultMap" type="com.shopping.entity.Cart">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="quantity" property="quantity"/>
        <result column="checked" property="checked"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="CartVOResultMap" type="com.shopping.vo.CartVO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="product_price" property="productPrice"/>
        <result column="quantity" property="quantity"/>
        <result column="checked" property="checked"/>
        <result column="stock" property="stock"/>
        <result column="total_price" property="totalPrice"/>
    </resultMap>

    <insert id="insert" parameterType="Cart" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart (user_id, product_id, quantity, checked)
        VALUES (#{userId}, #{productId}, #{quantity}, #{checked})
    </insert>

    <update id="updateById" parameterType="Cart">
        UPDATE cart
        <set>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="checked != null">checked = #{checked},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM cart WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM cart WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectById" parameterType="long" resultMap="BaseResultMap">
        SELECT * FROM cart WHERE id = #{id}
    </select>

    <select id="selectByUserIdAndProductId" resultMap="BaseResultMap">
        SELECT * FROM cart WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <select id="selectByUserId" parameterType="long" resultMap="CartVOResultMap">
        SELECT 
            c.id,
            c.user_id,
            c.product_id,
            p.name AS product_name,
            p.main_image AS product_image,
            p.price AS product_price,
            c.quantity,
            c.checked,
            p.stock,
            (p.price * c.quantity) AS total_price
        FROM cart c
        LEFT JOIN product p ON c.product_id = p.id
        WHERE c.user_id = #{userId}
        ORDER BY c.create_time DESC
    </select>

    <select id="selectCheckedByUserId" parameterType="long" resultMap="CartVOResultMap">
        SELECT 
            c.id,
            c.user_id,
            c.product_id,
            p.name AS product_name,
            p.main_image AS product_image,
            p.price AS product_price,
            c.quantity,
            c.checked,
            p.stock,
            (p.price * c.quantity) AS total_price
        FROM cart c
        LEFT JOIN product p ON c.product_id = p.id
        WHERE c.user_id = #{userId} AND c.checked = 1
        ORDER BY c.create_time DESC
    </select>

    <select id="selectByIds" resultMap="CartVOResultMap">
        SELECT 
            c.id,
            c.user_id,
            c.product_id,
            p.name AS product_name,
            p.main_image AS product_image,
            p.price AS product_price,
            c.quantity,
            c.checked,
            p.stock,
            (p.price * c.quantity) AS total_price
        FROM cart c
        LEFT JOIN product p ON c.product_id = p.id
        WHERE c.id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <update id="updateQuantity">
        UPDATE cart SET quantity = #{quantity} WHERE id = #{id}
    </update>

    <update id="updateChecked">
        UPDATE cart SET checked = #{checked} WHERE id = #{id}
    </update>

    <update id="updateAllChecked">
        UPDATE cart SET checked = #{checked} WHERE user_id = #{userId}
    </update>

</mapper>
