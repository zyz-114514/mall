<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shopping.mapper.ProductMapper">

    <resultMap id="BaseResultMap" type="com.shopping.entity.Product">
        <id column="id" property="id"/>
        <result column="category_id" property="categoryId"/>
        <result column="name" property="name"/>
        <result column="subtitle" property="subtitle"/>
        <result column="main_image" property="mainImage"/>
        <result column="detail" property="detail"/>
        <result column="price" property="price"/>
        <result column="stock" property="stock"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (category_id, name, subtitle, main_image, detail, price, stock, status)
        VALUES (#{categoryId}, #{name}, #{subtitle}, #{mainImage}, #{detail}, #{price}, #{stock}, #{status})
    </insert>

    <update id="updateById" parameterType="Product">
        UPDATE product
        <set>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="subtitle != null">subtitle = #{subtitle},</if>
            <if test="mainImage != null">main_image = #{mainImage},</if>
            <if test="detail != null">detail = #{detail},</if>
            <if test="price != null">price = #{price},</if>
            <if test="stock != null">stock = #{stock},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM product WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="long" resultMap="BaseResultMap">
        SELECT * FROM product WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM product WHERE status = 1 ORDER BY create_time DESC
    </select>

    <select id="selectByCategoryId" parameterType="long" resultMap="BaseResultMap">
        SELECT * FROM product WHERE category_id = #{categoryId} AND status = 1 ORDER BY create_time DESC
    </select>

    <select id="selectByNameLike" parameterType="string" resultMap="BaseResultMap">
        SELECT * FROM product WHERE name LIKE CONCAT('%', #{name}, '%') AND status = 1 ORDER BY create_time DESC
    </select>

    <update id="updateStock">
        UPDATE product SET stock = stock - #{quantity} WHERE id = #{id} AND stock >= #{quantity}
    </update>

</mapper>
