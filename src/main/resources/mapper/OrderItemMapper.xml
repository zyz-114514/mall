<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shopping.mapper.OrderItemMapper">

    <resultMap id="BaseResultMap" type="com.shopping.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="order_no" property="orderNo"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="current_price" property="currentPrice"/>
        <result column="quantity" property="quantity"/>
        <result column="total_price" property="totalPrice"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="OrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, order_no, product_id, product_name, product_image, current_price, quantity, total_price)
        VALUES (#{orderId}, #{orderNo}, #{productId}, #{productName}, #{productImage}, #{currentPrice}, #{quantity}, #{totalPrice})
    </insert>

    <insert id="batchInsert" parameterType="list">
        INSERT INTO order_item (order_id, order_no, product_id, product_name, product_image, current_price, quantity, total_price)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.orderNo}, #{item.productId}, #{item.productName}, #{item.productImage}, #{item.currentPrice}, #{item.quantity}, #{item.totalPrice})
        </foreach>
    </insert>

    <delete id="deleteById" parameterType="long">
        DELETE FROM order_item WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="long" resultMap="BaseResultMap">
        SELECT * FROM order_item WHERE id = #{id}
    </select>

    <select id="selectByOrderId" parameterType="long" resultMap="BaseResultMap">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>

    <select id="selectByOrderNo" parameterType="string" resultMap="BaseResultMap">
        SELECT * FROM order_item WHERE order_no = #{orderNo}
    </select>

</mapper>
